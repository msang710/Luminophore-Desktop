#!/bin/sh
set -eu
project_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
export PYTHONPATH="${project_root}${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m luminophore_shell.session_theme_cli install-session "$@"
