-- Luminophore-owned semantic compositor palette.
-- These fallback values preserve the last accepted desktop appearance until
-- the Luminophore system-theme transaction explicitly replaces them.

return {
    primary = "rgb(545a92)",
    surface_container = "rgb(fbf8ff)",
    secondary = "rgb(5c5d72)",
    error = "rgb(ba1a1a)",
}
